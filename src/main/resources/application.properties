application.title = CarShop SpringBoot Project Zsa Zsa

# property strings / messages
message.default.welcome = Welcome, I'm glad to see you're doing well!
message.default.goodbye = Goodbye, have a nice day!

# logging level can be =  "TRACE", "DEBUG", "INFO", "WARN", "ERROR", "FATAL", "OFF"
logging.info.root = INFO

#--# PostgreSQl DATABASE #--#
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.platform=postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=AOR99dfhZZ
spring.jpa.database=postgresql
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Hibernate
spring.jpa.hibernate.ddl-auto=create
spring.datasource.initialization-mode=always

# https
#
# self-signed certificate generated with:
# keytool -genkey -keying RSA -alias certificate -keystore certificate.jks -storepass password -validity 365 -keysize 4096 -storetype pkcs12
server.ssl.key-store=classpath:certificate.jks
server.ssl.key-store-type=pkcs12
server.ssl.key-store-password=password
server.ssl.key-password=password
server.ssl.key-alias=certificate
server.port=8443
